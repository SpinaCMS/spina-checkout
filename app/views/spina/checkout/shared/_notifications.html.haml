- if current_order.errors.any?
  .notification.notification-danger
    .notification-message!= current_order.errors.full_messages.join("<br />")

- if flash[:notification].present?
  .notification.notification-info
    .notification-message!= flash[:notification]